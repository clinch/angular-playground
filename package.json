{
  "repository": {
    "type": "git",
    "url": "https://github.com/clinch/angular-playground.git"
  },
  "name": "angular-playground",
  "version": "0.0.1",
  "dependencies": {},
  "devDependencies": {
    "karma": ">=0.12"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}
